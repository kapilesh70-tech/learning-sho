import CTA from "../components/CTA";

export default function FinalCTA() {
  return (
    <CTA
      eyebrow="Let's start a conversation"
      title={["Building capability", "is a business decision."]}
      body="If your organisation is building an L&D function, strengthening leadership, improving sales capability, transforming culture or exploring AI-enabled learning — let's talk about the challenge first."
      buttonLabel="Start a Conversation"
      buttonTo="/contact"
    />
  );
}
